import "package:flutter/material.dart";

class SaldoHistoryTab extends StatelessWidget {

  const SaldoHistoryTab({ super.key });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("History Saldo"),
    );
  }
}