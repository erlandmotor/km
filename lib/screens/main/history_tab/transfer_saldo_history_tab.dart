import "package:flutter/material.dart";

class TransferSaldoHistoryTab extends StatelessWidget {

  const TransferSaldoHistoryTab({ super.key });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("History Transfer Saldo"),
    );
  }
}