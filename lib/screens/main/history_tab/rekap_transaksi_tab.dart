import "package:flutter/material.dart";

class RekapTransaksiTab extends StatelessWidget {

  const RekapTransaksiTab({ super.key });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("History Saldo"),
    );
  }
}