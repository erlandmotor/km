import "package:flutter/material.dart";

class TopupSaldoHistoryTab extends StatelessWidget {

  const TopupSaldoHistoryTab({ super.key });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Topup Saldo History Tab"),
    );
  }
}